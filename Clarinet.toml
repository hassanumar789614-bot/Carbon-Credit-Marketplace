[project]
name = 'Carbon-Credit-Marketplace'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.credit-tokenization-system]
path = 'contracts/credit-tokenization-system.clar'
clarity_version = 3
epoch = '3.2'

[contracts.emission-offset-calculator]
path = 'contracts/emission-offset-calculator.clar'
clarity_version = 3
epoch = 'latest'

[contracts.satellite-verification-oracle]
path = 'contracts/satellite-verification-oracle.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
